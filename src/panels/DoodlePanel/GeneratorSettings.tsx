import { Stack } from "@mui/material";

export function GeneratorSettings() {
  return (
    <Stack>
      Prompts, templates, dimensions, sliders, etc.
      Didn't I already do this as panels though?
    </Stack>
  )
}
